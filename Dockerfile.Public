# Dockerfile
ARG DOTNET_VERSION=10.0
ARG BUILD_CONFIGURATION=Release

FROM mcr.microsoft.com/dotnet/sdk:$DOTNET_VERSION AS build

WORKDIR /src
COPY LDS.Web.Public LDS.Web.Public
COPY LDS.Web.Shared LDS.Web.Shared
COPY LDS.Data LDS.Data

WORKDIR /src/LDS.Web.Public
RUN dotnet restore "./LDS.Web.Public.csproj"
RUN dotnet build "./LDS.Web.Public.csproj" -c "$BUILD_CONFIGURATION" -o /app/build

RUN dotnet publish "./LDS.Web.Public.csproj" -c "$BUILD_CONFIGURATION" -o /app/publish -r linux-musl-x64

FROM mcr.microsoft.com/dotnet/aspnet:$DOTNET_VERSION AS run
WORKDIR /app
COPY --from=build /app/publish .
ENTRYPOINT [ "dotnet", "LDS.Web.Public.dll" ]